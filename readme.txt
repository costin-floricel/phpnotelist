The Application allows a registered user to add, view and delete notes.

-It Assumes that a user is already registered
-A login page is used in order to login
-the following credentials are used in order to login: 
	username: costin@costin.com
	password: testpassword

